pub fn alien_order(words: Vec<String>) -> String {
    let ranks: Vec<u32> = vec![0; 26];
    let words: Vec<Vec<char>> = words.iter().map(|word| word.chars().collect()).collect();

    unimplemented!()
}
